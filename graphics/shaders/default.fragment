varying vec4 VaryingInstanceColor;

vec4 effect(vec4 color, Image tex, vec2 texture_coords, vec2 screen_coords)
{
    //vec4 texcolor = Texel(tex, texture_coords);
    //return texcolor * VaryingInstanceColor;
    return vec4(0.);
}
