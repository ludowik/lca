FULLSCREEN = 'fullscreen'

viewer = {
    mode = FULLSCREEN,
    restart = restart
}

layout = {
    safeArea = {
        top    = X,
        bottom = Y,
        left   = X * 2 + W,
        right  = Y * 2 + H,
    }
}
