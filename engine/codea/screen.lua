FULLSCREEN = 'fullscreen'

viewer = {
    mode = FULLSCREEN,
    restart = restart
}

layout = {
    safeArea = {
        top = safeArea.top,
        bottom = safeArea.bottom,
        left = safeArea.left,
        right = safeArea.right,
    }
}
